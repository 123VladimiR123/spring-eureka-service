server:
  port: 9000

spring:
  application:
    name: eureka

  profiles:
    active: dev

  config:
    import: configserver:http://${CONFIG_SERVER_IP}:${CONFIG_SERVER_PORT}}

  cloud:
    config:
      fail-fast: true
